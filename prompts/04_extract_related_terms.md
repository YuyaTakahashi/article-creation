以下のテキストにある##関連用語の箇条書きになっている用語を抽出し、純粋なJSON配列形式で返してください。

# 制約事項
- Markdown記法（```json 等）は絶対に使用しないでください。
- 「以下が結果です」などの説明も不要です。
- [ で始まり ] で終わる配列のみを出力してください。

# 出力例
["用語A", "用語B", "用語C"]

[ テキスト ]
{{#1769304191920.text#}}
